#pragma once
namespace status {
	//Soft globals
	inline bool running = true;
	// Rendering globals
	inline bool renderEsp = false;
	inline bool renderMenu = false;
	inline bool renderRadar = false;

	// Hack globals
	inline bool aimbotEnabled = true;
}